syntax = "proto2";

package self.ed.proto;

message DefaultMessage {
    enum Type {
        A = 0;
        B = 1;
        C = 2;
    }
    optional int32 id = 1 [default = 123];
    optional Type type = 2 [default = B];
}