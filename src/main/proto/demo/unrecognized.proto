syntax = "proto3";

package self.ed.proto.unreq;

option java_multiple_files = true;

enum Status {
    UNSPECIFIED = 0;
    ACTIVE = 1;
    INACTIVE = 2;
}

message User {
    Status status = 1;
}